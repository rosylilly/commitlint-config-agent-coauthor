# Don't let pnpm self-manage its version via the lockfile. That feature records
# a `packageManagerDependencies` section in a second YAML document inside
# pnpm-lock.yaml, which Dependabot's parser can't read ("not parseable"). The
# pnpm version is still pinned by the packageManager field in package.json.
manage-package-manager-versions=false
